Когда вводим теги в ручную-------------------------------------------------------------
1)Все таблицы тегов для истории называются 'tegs_story_' + data_stories.story_id
Колличество таблиц равно колличеству историй.

2)Выбирать при помощи группировки sql:

       story_id  |  story_tags   |  tags_id | tags_text
	   id1      tableName1	     id1       value
	   null 	null	     id2       value
	   null	 	null	     id3       value
	   id2      tableName2	     id1       value
	   null	 	null	     id2       value
	   null	 	null	     id3       value

3)Сделали выборку из всех таблиц. 
А дальше преобразовываем в отдельной функции это в объект 
{
  "data_stories.story_id" => array(teg1, teg2,...,tegN)
}
4)После этого получаем тег, по которому ищем - search_tag1
и смотрим его во всех массивах с тегами в объекте тегов историй.
Если такой тег в массиве существует - передаем объект(можно скинуть во временный массив):
{ tagName: search_tag1, res: ["data_stories.story_id"1,...,"data_stories.story_id"N] }
и продолжаем искать дальше.
Во временном массиве будут все названия историй, у которых есть search_tag1

5)Если тегов несколько(сделаем универсально - то есть даже если тег один),
то делаем повторение шага(4) колличество раз, равное колличеству тегов в запросе
(рассчитать колличество тегов и отделить их друг от друга в отдельной функции).

6)В итоге мы получаем новую кучу массивов с объектами шага(5), 
ну а дальше проводим сортировку и благодаря ней делаем конечны результирующий объект
result для вывода его на echo для Ajax, в котором сначала будут те истории, в которых содержится
больше всего введенных тегов и по убывании.

7)На стороне js мы получаем Ajax-ответ, в котором будут содержаться все нам нужные данные, и благодаря нему
мы формируем блоки с ссылками на историю, колличество которых равно колличеству элементов объекта Ajax-ответа


Когда кликаем теги в историях мышкой---------------------------------------------------
1)


---------------------------------------------------

$arr_buttonPagination = array();
		for($i = 0; $i < $index; $i++){
			$arr_buttonPagination['i_'.$i] = array();
			
			$counter = 0;
			for($j = $i; $j < $index; $j++){
				$arr_buttonPagination['i_'.$i]['j_'.$j] = $j;
				$counter++;
				if($counter >= NUM_PAGES){ break; };
			};
			$i += NUM_PAGES - 1;
		};
		/*
			Еще вариант реализации пагинации:
			присваиваем атрибут(к примеру numPag) кнопкам пагинации:
			1*NUM_PAGES, 2*NUM_PAGES, 3*NUM_PAGES и так далее, где первое число - это цифра кнопки.
			В итоге мы будем искать в бд истории в диапазоне от 12 - 6 до 11 если мы нажали кнопку "2". 
			Можно указать диапазон в этом атрибуте кнопки.

			А можно поступить еще проще - просто получать цифру кнопки. 
			И благодаря ней самому определять диапазон :3
		*/

